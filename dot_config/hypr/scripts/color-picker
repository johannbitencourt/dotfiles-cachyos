#!/usr/bin/env bash
set -euo pipefail

if ! command -v hyprpicker >/dev/null 2>&1; then
  notify-send "Color picker unavailable" "Install hyprpicker from packages/apps-pacman.txt"
  exit 1
fi

pkill hyprpicker || hyprpicker -a
